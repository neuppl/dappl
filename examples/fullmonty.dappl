door1 <- flip 0.333 ; 
door2 <- ((!door1) && (flip 0.5)) ;
monty <- (if door1 then (flip 0.5) else door2);
observe(monty);
choose [switch, no_switch] with
| switch -> if door2 then reward 500 else reward 0
| no_switch -> if door1 then reward 500 else reward 0