x <- (flip 0.5) ;
y <- [a,b] ;
loop 3 {
  if x then
    (choose y with
    | a -> (reward 3 ; return tt)
    | b -> return tt)
  else
    (choose y with
    | a -> (reward 3 ; return tt)
    | b -> return tt)
}
