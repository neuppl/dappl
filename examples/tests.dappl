bronc <- flip 0.5;
either <- flip 0.5;
dysp <- (if bronc then (if either then (flip 0.9) else (flip 0.8))
        else (if either then (flip 0.7) else (flip 0.1)));
if dysp then reward 1 else reward 0