f1 <- (flip 0.1) ;
if f1 then
  (f2 <- (flip 0.1) ;
  if f2 then
    (return (ff))
  else
    (D4 <- ([d310]) ;
    choose D4 with 
    | d310 ->
      (return (ff))))
else
  (D7 <- ([d51 , d65]) ;
  choose D7 with 
  | d51 ->
    (return (ff))
  | d65 ->
    (f9 <- (flip 0.1) ;
    if f9 then
      (return (ff))
    else
      (D11 <- ([d1010]) ;
      choose D11 with 
      | d1010 ->
        (return (ff)))))