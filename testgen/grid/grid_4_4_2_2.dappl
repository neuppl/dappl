f1 <- (flip 0.1) ;
if f1 then
  (f2 <- (flip 0.1) ;
  if f2 then
    (return (ff))
  else
    (D5 <- ([d30 , d45]) ;
    choose D5 with 
    | d30 ->
      (return (ff))
    | d45 ->
      (return (ff))))
else
  (D7 <- ([d61]) ;
  choose D7 with 
  | d61 ->
    (f8 <- (flip 0.1) ;
    if f8 then
      (return (ff))
    else
      (D11 <- ([d90 , d105]) ;
      choose D11 with 
      | d90 ->
        (return (ff))
      | d105 ->
        (return (ff)))))