f1 <- (flip 0.1) ;
if f1 then
  (f2 <- (flip 0.1) ;
  if f2 then
    (return (ff))
  else
    (D4 <- ([d32]) ;
    choose D4 with 
    | d32 ->
      (return (ff))))
else
  (D6 <- ([d51]) ;
  choose D6 with 
  | d51 ->
    (f7 <- (flip 0.1) ;
    if f7 then
      (return (ff))
    else
      (D9 <- ([d82]) ;
      choose D9 with 
      | d82 ->
        (return (ff)))))